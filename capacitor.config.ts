import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kindserviceplatform.app',
  appName: 'Kind Service Platform',
  webDir: 'dist'
};

export default config;
